name: dupplifilter
description: App Flutter DuppliFilter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.2.5
  image: ^4.1.3
  share_plus: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true